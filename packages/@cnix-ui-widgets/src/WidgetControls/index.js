import React, { forwardRef } from 'react';
// import makeStyles from '@';
import Widget from '@cnix-ui/core/Widget/Widget';
import WidgetHeader from '@cnix-ui/core/Widget/WidgetHeader';
import WidgetContent from '@cnix-ui/core/Widget/WidgetContent';

const WidgetControls = () => {
  const classes = {};
  return (
    <Widget>
      <WidgetHeader />
      <WidgetContent />
    </Widget>
  );
};

// export default WidgetControls;

export default () => <div>Pepe</div>;
